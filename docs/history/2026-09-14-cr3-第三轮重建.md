# CR3 · 第三轮全项目 code review（2026-09-14）—— 从 git 重建

> **⚠️ 本文件是重建产物。** 第三轮审查在 `PLAN.md`、`Progress.md`、`code-review.md` **三处均无任何记载**，
> 现有全部信息来自提交 `d6ac165`（2026-09-14 23:45:49 +0800）的 message 与 diff。
> 归档时点在文档整理（2026-09-20），此后如找到当时的审查原文，应以原文为准并更新本文件。

## 为什么这一轮重要

该轮是**本项目"修复只做了一半"模式的源头**——它自己修掉的问题里就包含 1 项由上一轮修复引入的**回归**和 1 项**不完整修复**。
此后 CR4→CR5（C11→CR4-4、CR4-6→CR5-3）与 CR7-1 反复出现同一模式。

## 8 项发现（逐条，按 commit message 原文整理）

### 高风险（含回归）

1. **`research/ingest` 类型白名单误限 `stock`/`us`（上一轮修复引入的回归）**
   `fund`/`bond`/`crypto`/`hk` 研报完成回调必然 400 → `ResearchReport` 永久 running、前端无限轮询、**研报静默丢失**。
   → 修复：删除白名单，仅保留 `type` 非空校验。
   *（注：把校验收得过窄本身也是一种"修复过头"。此条与 CR-26/C33 的入参白名单约束形成张力——白名单要覆盖**全部**合法值。）*

2. **`trimContext` 溢出兜底不完整（上一轮修复的不完整之处）**
   溢出点落在 `assistant`/`tool`（多轮工具循环末尾）时 `kept=[]` 整段丢弃、**用户提问消失**。
   → 修复：改为保留含溢出点的整轮，并硬截断超限单条。
   *（即 PLAN 中 C18 那句"最后一条消息自身超预算时硬截断保留，禁止整条丢弃"的来源。）*

### 中风险

3. **chat 意图升档 `[A-Z]{3,5}` 把 `ETF`/`IPO`/`GDP` 等缩写误判为美股代码**
   → 修复：抽出 `web/lib/research-target.ts`，加停用词表排除。

4. **`market-snapshot` inflight 未挂 `globalThis`（违反 C17）** → 修复。

5. **`syncType` 单飞只串行不合并**：并发 N 次会跑 N 次全量 → 改为 `return inflight`。

6. **`backup_db restore` 回滚丢失 `-wal`/`-shm`** → 记录全部挪动文件并按序还原。
   *（C22 的"清理目标库的 `-wal/-shm` 残留"与这条同源。）*

### 低风险

7. **`gateway` `statusCache` 未挂 `globalThis`（违反 C17）** → 修复。
8. **`openbb_provider.get_news` 签名陈旧**：`-> dict` → `-> list[dict]`（对齐 C5 契约）。

## 改动文件（11 个，+193/−43）

```
改    data-service/app/providers/openbb_provider.py      (+7)
改    data-service/scripts/backup_db.py                  (+40/-…)
改    web/app/api/chat/route.ts                          (10)
改    web/app/api/research/ingest/route.ts               (12)
改    web/lib/context-budget.ts                          (26)
改    web/lib/gateway.ts                                 (17)
改    web/lib/market-snapshot.ts                         (10)
改    web/lib/sync.ts                                    (9)
新增  web/lib/context-budget.test.ts                     (+55)
新增  web/lib/research-target.ts                         (+20)
新增  web/lib/research-target.test.ts                    (+30)
```

## 验证

- `tsc` 0 错
- `vitest` **73/73**（新增 8 项）
- data-service 离线 **45/45**
- backup 回滚失败路径**手工验证**（三文件还原、无残留、原数据完好）

## 取回原始信息

```bash
git show d6ac165                    # 完整 diff
git log -1 --format=%B d6ac165      # 完整 commit message
```
