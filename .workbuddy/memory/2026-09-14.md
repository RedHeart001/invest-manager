# 2026-09-14 工作日志

## 项目状态确认（17:26，主人询问"整个项目任务进行如何"；按约定逐项实测）

**实测结论**

- **文档**：Progress 状态总览 P0–P7 全 ✅（P2 已 38/38）——与磁盘代码一致，无虚报
- **git**：4 次提交（b22671f 基线 155 文件 / f5656d7 转债闭环+2 P0 / e6b00f5 docs / 3bd73a1 第二轮审查 12 项）；`git status` 干净，无未提交改动
- **代码**：O 系列 7 个新模块全在位；L2 改动在位（prisma 已在 dependencies、Dockerfile 含 `npm prune --omit=dev`）；data-service 25 个 py 文件语法全 OK
- **测试（本次实跑）**：web 单测 **65/65** ✅；data-service 离线 **45/45** ✅（p2_m8 23 + p6_fund_report 15 + p6_mcp 7）；`test_p0.py`/`test_p1.py` 属集成测试需 data-service 在跑，当前服务未启动故无法执行
- **DB**：`web/prisma/dev.db` 29.6MB（09-13 18:09 后未变动）
- **规模**：源码 144 文件（ts/tsx/py/mjs）、git 跟踪 156 文件

**当前未运行**：web dev(3000)、data-service(8000)、Docker 守护进程——三个均已随会话结束停止；Docker Desktop 未启动（npipe 不存在）

**唯一待办（需主人操作）**：🟡 L2 web 镜像瘦身验证——启动 Docker Desktop 后 `docker compose build web` → `up -d` → `node scripts/smoke.mjs`，记录实际体积（预期 ~0.9GB）

**环境级限制（非缺陷）**：东财 IP 级滚动限流；转债 K 线仅东财一源，限流时按 R10/R12 显式降级即正确行为

## L2 镜像瘦身验证闭环（17:40–18:0x，Docker Desktop 已由主人启动 v29.7.2）

主人问"这条命令是不是生成镜像文件"，顺势把挂起的 L2 验证做完，**过程中发现并修复 2 个真缺陷**：

1. **[高] prisma CLI 被误裁** → 只改了 `package.json` 未同步 `package-lock.json`，`npm prune --omit=dev` 以锁文件为准把 prisma 当 devDep 删掉 → `migrate deploy` 必失败。修复：`npm install --package-lock-only`（比对确认无版本漂移）。**固化 C24**
2. **[中] 裁剪位置错误** → runner 内"先 COPY 再 prune"，层语义下被删文件仍占体积（实测仍 1.62GB）。修复：独立 `prod-deps` stage 裁剪。**固化 C25**
3. **额外**：裁掉 musl/wasm 平台变体（`@next/swc-linux-x64-musl` 137MB + sharp musl/wasm 28MB）再省 165MB

**实测**：`docker images` 1.62GB → **1.25GB**（-23%）；容器内占用 **0.89GB**（原 ~1.29GB）；两容器 healthy；冒烟 **8/8**。
**认知纠正**：devDeps 不是体积大头（裁剪后 node_modules 仍 833MB，主体是 @next SWC / next / @prisma / echarts / sharp）。PLAN 原"预期 ~0.9GB"口径有误，已在 PLAN/Progress 更正。

**未做（评估记录）**：prisma migrate 拆 one-shot 服务（省 67MB）；去掉 @img/sharp（项目未用 next/image，省 46MB，需验证 next start 不强制加载）。
