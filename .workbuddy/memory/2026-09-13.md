# 2026-09-13 工作日志

## 项目进度全量盘点（只读体检）

对 PLAN.md / Progress.md 与实际代码逐一核对，结论：文档与代码一致，无虚报。

- 代码落地确认：data-service 20 个 py 模块（providers 5 + hotspot 3 + research 3 + utils/config/main）；web 16 个 API route、lib 20 个模块、5 个测试脚本（test-db/p1/p2/p3/p4/p5 + b2-chain）。
- 核心引擎规模：research/engine.py 276 行、adapter.py 277 行、tasks.py 149 行；web tools.ts 381 行（L1 6 + L2 2 = AGENT_TOOLS 8 个工具齐全）。
- **P0–P5 已全部完成**（P2 标 ⚠️，遗留 2 项转债 K 线为外部源问题）。P6（Skills+MCP）、P7（Docker）均未开始 —— 目录中确实无 skills/、mcp.json、Dockerfile、docker-compose.yml（Docker 骨架验证后已按用户要求清除）。
- 当前配置：web/.env 只配了 DATABASE_URL / DATA_SERVICE_URL / TAVILY_API_KEY / LLM_API_KEY / LLM_MODEL（缺 LLM_BASE_URL，靠代码按模型名推断）。
- 双服务当前均未运行（8000 / 3000 无监听）。
- 环境注意：本机 Git Bash 缺少 coreutils（head/wc/ls 等不可用），Bash 工具基本不可用，须改用 PowerShell；PowerShell 的 stdout 有时不被捕获，需写临时文件再读（读完记得删）。

## 下一步

待用户批准 P6：Skills 加载器（`skills/<name>/SKILL.md`，启动仅注入元信息、命中再注入正文）+ MCP client（HTTP 优先）+ fastmcp 暴露统一行情 API。之后 P7 Docker 化（参考 09-12 日志中的构建参数对策重建）。

## P6/P7 二轮方案评估定稿（用户已确认四项推荐）

- M7：Tool Gateway 内部命名空间但 LLM 工具名不变（test-p4 回归红线）；技能=纯 prompt 编排包；首批技能重排 hotspot-daily→tech-indicators→fund-report-analysis(spike 先行)；MCP client MVP 只接 1 个 stdio server；fastmcp 只暴露自有只读能力、search 走 BFF 回调。
- P7：requirements 全量 freeze（裁掉 tradingagents/OpenBB SDK，只锁 yfinance）；单镜像；SQLite 备份脚本；compose 强制 INGEST_TOKEN；冒烟五项。
- 已全部写入 PLAN.md（M7 二轮补强节 + P7 二轮补强 + 验证方式二轮增补）与 Progress.md 当日日志。仍未开始执行，等"开始 P6"指令。

## P6 完成（当日 09:35 起执行，验收 21/21）

交付：`web/lib/gateway.ts`（三分命名空间）、`web/lib/skills.ts` + `web/skills/*`（3 个技能）、`web/lib/mcp.ts`（MCP client）、`data-service/app/mcp_server.py`（fastmcp，10 只读工具）、`/api/tools/status`、L1 新增 `get_fund_report` + data-service `/fund/report`、`web/scripts/mcp-local-util.mjs`（本地 stdio MCP server）。测试：`web/scripts/test-p6.mjs` 21/21、单测 64/64、`tests/test_p6_mcp.py` 7/7、`tests/test_p6_fund_report.py` 15/15、test-p4 回归 19/19。

实证：LLM 自主调用 MCP 工具 `mcp_local_util_local_now` 并正确回答交易时段；基金技能命中后如实报告数据缺口而非编造。

修复缺陷：`_num` NaN 导致非法 JSON 与过滤失效（已全局修）、mcpStatus 不反映 disabled、状态面板不探测连接、search_products 的 import 在 try 外、requirements 漏 yfinance。

环境事故：fastmcp 4.x 与 fastapi/starlette 版本冲突 + pip 卸载被沙箱守卫拦截 → venv 损坏 → 重建 venv 修复。详见 MEMORY.md「环境与工具链」节。

## 垃圾清理（11:03，用户要求）

已清除 P6 任务产生的全部临时物：`web/tsconfig.tsbuildinfo`（tsc 增量缓存）+ TEMP 中 15 个任务日志（p6probe/fastmcp_install/httpx_install/fastmcp_fix×3/venv_rebuild/quarantine/cleanup/nextreset×2/junkscan/ds-p6/ds-p6b/web-p6）；此前已清 spike 脚本与结果、临时探针、`.next-stale`、`.venv-broken`。`.gitignore` 补充 `*.tsbuildinfo`、`.venv-spike/`、`*.pyo`（`*.log` 原本已有）。

**历史遗留垃圾（用户 11:08 确认后一并清除）**：`web/b2-out.txt`（P5 临时输出）、`web/dev-server.log`、`data-service/ds-server18.log`。至此项目根/web/data-service 三处均无临时产物，仅剩正常交付物与 venv/node_modules/.next/prisma 等运行所需目录。

## P6 深度体检（11:12 评估完成）

- 验证补齐：MCP server HTTP 传输（8765）实测通过（10 工具可调）。
- 修复 2 项高风险：① MCP 运行时注册表挂 globalThis（防 Next HMR 重复 spawn 孤儿进程）+ exit hook 清理；② ensureConnected 并发 in-flight 去重。
- 补齐：requirements 显式钉 requests/pandas；.env.example 全量化；PLAN P7 新增 skills/mcp.json 随镜像分发条款。
- 回归：vitest 65/65、test-p6 21/21、tsc 0 错误；临时文件已清。
- 结论：P6 可验收；遗留低优先优化点（触发词误命中、状态面板超时边界、技能缓存无界）已记录。

## 体检结论整合入文档（11:43，用户要求）

- **PLAN.md**：M7 ② 改写为"实现方式定稿"（记录偏离：自研 MCP client 而非官方 SDK + 理由）；M7 新增「落地定稿与实现约束」（8 条不得回退的工程约束，含 globalThis HMR 安全、in-flight 去重、NO_PROXY 直连、状态面板探测式、MCP server 只读）与「已知优化点」3 条；「配置项（.env）」补全为 P3–P7 全量（原缺 SKILL_MAX_*、MCP_*、INGEST_TOKEN、WEB_API_BASE 等）；风险节补 requests/pandas 显式钉版；验证方式新增「P6 三轮增补」4 项；P7 补 skills/mcp.json 随镜像分发。
- **Progress.md**：P6 深度体检条目补"PLAN 同步修正（实现偏离记录）"；「阻塞与问题」新增 🟢 P6 已知优化点（3 条，非阻塞）；状态表 P6 单测数更新为 65/65。
- 本次仅改文档，无垃圾文件产生（已核查）。

## P7 方案三轮评估（11:55，评估+调整，未开发）

关键发现：① P6 落地后 data-service→web 回调增至 4 条且默认 localhost——容器内必挂，P7 必须接 `WEB_BASE_URL=http://web:3000`；且回调变量名不统一（WEB_BASE_URL vs WEB_API_BASE，小缺陷，P7 实施时兼容）。② MCP 容器化陷阱：容器内绑 127.0.0.1 宿主映射也访问不到 → 默认不进容器，可选 compose profile `mcp`（容器内 0.0.0.0）。③ slim 无 curl 的 healthcheck 命令 + web `/api/health` 需重建 + openssl 补装落档。④ 具体化：pip freeze 锁版、sqlite3 在线备份 API、冒烟五项改 BFF×4+容器内 health、.dockerignore 红线。已写入 PLAN P7 三轮补强 9 条 + 验证增补 5 项 + Progress 当日日志。

## P7 Docker 化完成（12:09 起执行，验收全过）

交付：双 Dockerfile（web node:22-slim / data-service python:3.12-slim）、docker-entrypoint（migrate deploy）、docker-compose.yml（含 mcp profile）、双侧 .dockerignore、.gitattributes、requirements-lock.txt（容器内生成 114 包）、backup_db.py（sqlite3 在线备份）、scripts/smoke.mjs（8 断言）、web/app/api/health（重建）、根 .env.example、README 部署章节。

验收：双镜像 build、双服务 healthy、启动顺序（ds 等 web healthy）、**跨容器回调实证**（启动补跑 5 条 digest 落库）、BFF 全链路、TZ=CST、ingest 401/token 通过、镜像无 .env/dev.db 且含 skills+mcp.json、卷持久化（down/up 28897/5 不变）、**备份→全新卷恢复**（25.5MB 快照 → 空 0/0 → 恢复 28897/5）、mcp profile 宿主可达、冒烟 8/8。

修复缺陷：research ingest 缺 x-ingest-token 且不检查响应码（会静默丢研报）、mcp_server 变量名不统一、数据卷只读导致恢复写不了。

踩坑：锁文件必须与镜像 Python 同版本（3.12）且在容器内生成；base 阶段设 NODE_ENV=production 会让 npm ci 跳过 devDeps（缺 tailwind postcss / prisma CLI）；docker.io 认证被拦截 → daocloud 前缀。

收尾：容器已 down（**数据卷保留**含 28897 条数据）、验证备份已删、pipcache 卷保留供加速构建。环境条件：股票同步受东财限流失败、加密无代理不可达（非代码问题）。

## 全项目 code review（12:43 起）

方式：三路 Explore 代理并行（路由/页面、lib、data-service）+ 逐条人工验证后修复。**验证拦下 2 个误报**（scheduler GET 探测 405 不成立；sse 无心跳不成立——心跳在 stream 路由内）。

修复 14 类真实缺陷（已固化为 PLAN「全项目代码审查」C1–C14 不可回退约束），最重要的：
- C1 sync 空载荷静默清库；C2 会话取最早 200 条；C5 get_news list/dict 契约错配（东财恢复即打挂研报链，P5 期间被限流掩盖）；C9 前端直连 data-service（容器下按钮必挂）→ 新增 /api/hotspots/run；C6 限速器在状态校验前回报成功；C7 akshare 无超时（新 utils/timeout.py）；C8/C10 研报推送定向会话 + ingest 失败可观测；C3 LLM 尾帧 flush。

遗留：O1–O12 可优化点已列决策表进 PLAN，等用户逐条批复。

## O 系列实施计划→全部执行完成（14:31 起执行）

用户批准 B+M+L 全部执行。12 项全部落地：
- B 批 5 项（低风险）+ M 批 4 项（上下文预算/快照单飞/LRU/抓取异步化）+ L 批 3 项（sync 暂存表换名/镜像瘦身/采集线程治理）
- 执行中发现并修复 M4 初版死锁（request_run 置 running 后线程再走 _single_flight 直接返回 → running 永久卡死），拆出 _execute 直接执行
- 修正 test-p3 两处过时断言（"深度解读（P5 上线）"→"深度解读"；空查询断言匹配 R14 浏览模式语义）
- 验证：tsc 0 错、vitest 65/65、test-p1 19/20（转债富集为东财限流波动）、test-p3 28/28、test-p4 19/19、test-p6 21/21、data-service 离线 45/45、L1 实测通过、M4 实测 1.3s 返回
- 遗留：L2 web 镜像重建被 Docker Desktop 退出阻塞，重启 Docker 后 `docker compose build web` 即可验证

## O 系列实施计划（13:56，用户要求"先写计划不执行"）

已把 O1–O12 分批计划写入 PLAN「O 系列优化实施计划」：B 批 5 项（低风险）→ M 批 4 项（中风险）→ L 批 3 项（较大），每项含做法/范围/验证；每批回归门槛 tsc+vitest+集成套件全绿。状态：**待用户批准批次后执行**。

验证：tsc 0 错、vitest 65/65、test-p4 19/19、test-p6 21/21、data-service 离线 45/45、页面 200。插曲：批量补丁曾致 HotspotFeed 括号失衡（写入中间态被 watcher 撞上），tsc 确认自愈。

## 项目状态确认 + 修复 2 个缺陷（15:44 起）

**确认方式**：不凭记忆，逐项实测（文档/代码/产物/服务/测试/DB/配置）。结论已写入 PLAN（C15 新约束 + L2 描述修正）与 Progress（状态总览 + 日志 + 阻塞）。

**现状**：P0–P7 + C1–C15 + O1–O12 全部完成。开发态全绿：tsc 0 / vitest 65/65 / ds 离线 45/45 / test-db 16、test-p1 19（转债富集环境）、test-p3 28、test-p4 19、test-p6 21。DB：product 34777（含 us:1 AAPL）、kline 492、digest 22、研报 5。

**修复的两个缺陷**：
1. **[高] FTS 孤儿累积（L1 回归）**：rebuildFts 按"当前 Product id"删 FTS 行，而 L1 全量替换生成全新 id → 旧行永不删。实测 FTS 35828 vs Product 34777，**孤儿 1052 全为转债**（= 一次 bond 同步条数）。修复：rebuildFts 前置通用孤儿清理 + 暂存表改 DROP；一次性脚本 `web/scripts/fix-fts.mjs` 修数据（→ 34777 一致）；固化 **C15**。教训：**换名式替换主表后，任何以旧 id 为锚点的派生数据（FTS/关联表）都会失锚**。
2. **[中] test-p3 readUntil 竞态（测试缺陷）**：race 失败后丢弃 pending `reader.read()` → 事件偶发丢失（此前全绿属侥幸）。curl 独立验证产品链路完好（hello/digest/心跳）。修复为"保持唯一 pending read"。

**另修**：test-p1 FTS 断言口径未含 us 类型。

**未闭环**：① L2 镜像重建验证（Docker Desktop 未运行，需主人启动；Bash 侧不能拉 GUI）；② 转债行情/K 线（东财限流，无备源）；③ **项目未初始化 git**（无版本追溯，重要风险）。

**新增运维脚本**：`fix-fts.mjs`（FTS 一致性修复，可重复执行）、`verify-all.mjs`（全套件串行复验+汇总）——留在 web/scripts/。
