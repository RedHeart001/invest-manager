# 2026-09-12

## P2 方案讨论定稿（未编码）

- 用户确认了 P2 详情页设计三决策：① 变化归因分两步走（P2 做 ZigZag 阶段划分 + 大波动日事件标注，完整归因归 P5）；② 一句话特点画像用规则模板（不用 LLM）；③ 明细区长页全展开不折叠。
- 新增需求 R11（变化归因两阶段策略）已写入 PLAN.md；M3 节补充六区结构 + 按类型槽位表；P2 验证方式同步更新；Progress.md 记录定稿日志。
- 页面六区：身份 / 现状 / 主图 / 变化解读 / 明细 / 深度分析入口。归因文案一律"可能相关"非因果断言。
- 加密类型：无免费新闻源，仅做阶段划分并标注数据缺口；加密行情数据源（CoinGecko 不可达）仍待用户决策。
- 下一步：按定稿开始 P2 编码。

## 阶段约定（用户明确要求）

- 当前只做项目需求计划（方案讨论 + 文档维护），**不实际开发**。已写入 MEMORY.md 长期约定；何时开始编码等用户显式指示。

## P3–P5 优化点评估（待用户决策）

- 基于 P0/P1 实战教训（代理/CDN/限流/CoinGecko 不可达）与 P2 复用红利，评估出 P3×4、P4×4、P5×4 共 12 个优化点，核心逻辑是"每阶段为后续沉淀基础设施"（K线缓存/新闻provider/SSE/Tool Gateway）。
- 高风险 4 项待用户评估：H1 海外 API 不可达（Tavily/yfinance/AV 大概率同 CoinGecko 命运，建议国内源降级路径）；H2 LLM function calling 兼容性（尽早定目标 LLM）；H3 TradingAgents 集成不确定性（spike 先行 + B 计划）；H4 东财限流被 P3/P5 放大（P2 缓存是前置依赖）。
- 用户决策：12 点全部写入 PLAN.md；H1 采用"本地代理优先 + 不可达自动降级国内源并显式提示"（新增 R12，同时覆盖原挂起的加密数据源决策，东财数字货币源待验证）。
- 已更新：PLAN.md（R12、M1/M4/M5 补强块、风险节 4 项、P3–P5 验证增补）、Progress.md（状态表 P3–P5 备注 + 定稿日志）。H2 目标 LLM 选型仍待用户尽早确定。

## 目标 LLM 暂定（用户决策）

- 暂定 **DeepSeek + GLM（智谱）**：原生 function calling、OpenAI 兼容端点、国内可达（不受 H1 影响）、低成本（适配 TradingAgents 高频调用）。
- H2 降级为"P4 启动时双家 FC spike 验证"，工具抽象仍预留双模式兜底。已更新 PLAN.md 决策表与风险节、Progress.md 日志、MEMORY.md 挂起事项。

## 称呼偏好 + R13（用户决策）

- 用户要求称呼其为"主人"：已写入用户级 `USER.md` 与 `~/.workbuddy/MEMORY.md`（跨项目生效）。
- 数据源可用性验证推迟到落地阶段；用户期望**多源数据对比验证** → 新增 R13 入 PLAN.md（关键指标双源交叉验证，差异超阈值显式标注，不静默取舍；风险节同步）。
- Progress.md：加密数据源阻塞项标记已解决（R12 覆盖 + 验证推迟）；新增日志条目。挂起事项全部收敛，仅剩东财数字货币源落地验证。

## P6/P7 优化点评估（待用户决策）

- P6×7 点：MCP 传输 HTTP 优先（stdio 与容器化冲突）、Skills 安全边界（schema 校验/scripts 不自动执行）、上下文预算护栏具体化、技能热加载、首批技能按复用度排序（tech-indicators→hotspot-daily→fund-report-analysis）、Gateway 卫生与 /api/tools/status、fastmcp localhost 默认+token 限流。
- P7×7 点：SQLite named volume（不用 Windows bind mount）、entrypoint 自动 migrate deploy + 健康检查、TZ=Asia/Shanghai、ingest 共享密钥+端口不对外、slim 多阶段构建锁版本（openbb 体积文档化）、代理透传 host.docker.internal、一键冒烟脚本。
- 风险：MCP 白名单制不自动安装；镜像失控备选"核心+分析"双 tag；日常开发不进容器。
- 待用户决策：是否全部写入 PLAN.md；是否在 P2 后插"最小 Docker 骨架验证"（P7 内容不变）。

## P6/P7 评估结果（用户已确认）

- 14 点全部写入 PLAN.md：M7 补强块、实施阶段 P7 补强、风险节 2 项、验证方式 P6/P7 增补；Progress.md 状态表 + 日志同步。
- 用户确认 P2 验收后插入最小 Docker 骨架验证（P7 内容不变）。
- **至此 P0–P7 全部 8 个阶段方案评估完毕，规划期文档闭环**；下一步等主人指示开始 P2 编码。

## P2 编码执行（用户 2026-09-12 指示开工；重试上限上调为 3）

- P2 全部编码完成：data-service（kline 类型路由/净值历史/分时/新闻/持仓/收益率曲线/quote 扩展字段/crypto kline + **修复沪转债 secid 前缀 bug**）；web（KlineDaily 增量缓存、ZigZag 阶段划分、规则画像、事件映射、/api/kline、/api/events、详情页六区 + ECharts）。
- 验收：单测 36/36、tsc 零错误、test-p2 38 项断言 **35/38**；3 项失败为东财 IP 级限流（连未缓存股票也 502），20 分钟冷却后后台自动复跑（web/p2-final.txt）。
- R13 双源交叉验证（kline vs quote <0.5%）、缓存二次命中（fetchedDays=0）均通过。
- BTC/CoinGecko 直连不可达 → 显式降级；**待用户提供本地代理端口（HTTPS_PROXY）以启用加密图表**。
- 教训：东财限流冷却 >4 分钟，两轮测试即触发；P3 批量接口必须低频调度 + 强缓存。

## P2 收尾：代理链路打通 + 沙箱环境约束（重要发现）

- **用户代理端口 7897**。crypto 链路修复：requests 显式代理需 `trust_env=False` 独立 Session（免疫 main.py 的 NO_PROXY=*），provider 层实测 BTC price=77181 rank=1 ✓。
- **重大环境发现**：WorkBuddy 后台任务沙箱会改写进程代理 env（7897→其中继 57411）并拦截境外 CONNECT（502）。后台启动的 data-service 无法用 R12 海外源；国内源不受影响（NO_PROXY=* 直连）。**解法：主人在自己终端前台启动 data-service + 设 HTTPS_PROXY**（已写入 README）。
- 东财限流冷却超 20 分钟仍未恢复（4min/20min 两次自动复测均 STILL-THROTTLED）；test-p2 恢复后重跑即补齐 3 项。

## R14 分类浏览（P2 期间新增需求）

- 搜索页选中分类标签即浏览全部产品 + 涨幅/名称/代码排序 + 分页（用户 2026-09-12 截图反馈）。方案：Product 快照列（lastPrice/lastChangePct）做全局排序，当前页实时富集展示。
- **迁移教训**：`migrate dev` 漂移检测会删应用层 FTS5 虚表 → 手工迁移路线（db execute + migrate resolve）。**SQLite 教训**：Prisma 内置引擎不支持 UPDATE...FROM (VALUES) → 事务内批量 updateMany。
- 基金快照 23,953/27,811 已写入（ETF/LOF 待东财冷却）；股票/转债快照待冷却后 POST /api/market/refresh。
- 诊断类临时脚本用 .mjs 纯 JS（Node 22 不解析 .mjs 内 TS 标注）；正式代码保持 TS。

## Docker 骨架验证（完成 + 已清理）

- 五项全过：双镜像 build、空卷迁移自动应用、双容器 healthy、容器互通、卷持久化（rows=1）。构建对策已沉淀：docker.io 走 daocloud 镜像前缀；pip 阿里源 + timeout 120/retries 10；npm ci 长超时参数。Prisma slim 镜像 openssl 告警无害（P7 补装）。
- **用户要求验证后全清**：down -v --rmi local + 102 个文件删除，零残留。P7 重建清单即 Progress 09-12 日志"骨架验证"条目。
- 环境事实：Docker Desktop 29.7.2 可用（曾自退，用户手动启动后正常）；沙箱宿主侧仅白名单端口可达（3001 不可达，容器内自检绕过）；沙箱 trash 首批调用超时、重试即成功。
- EM 限流极顽固：45 分钟完全静默仍未恢复（按日级预估）。test-p2 剩 3 项，恢复后手动 `node scripts/test-p2.mjs`。

## P2 完成判定（用户确认）

- **P2 = ⚠️ 完成（2026-09-12）**：验收 36/38；遗留 2 项（沪/深转债 K 线）为外部源问题。
- **用户决策：东财链接问题记录搁置，P5 完成后集中测试**（P5 数据层落地时统一验证多源与限流策略）。已记入 Progress.md「阻塞与问题」。
- 多标的对比数据通路已补验（600519/000001 各 64 根）✓。
- 下一步：按 PLAN 顺序进入 **P3 热点 pipeline**（等主人指示）。

## P3 热点 pipeline 完成（2026-09-12，验收 27/27）

- data-service v0.5.0：hotspot 模块（pipeline/llm_client/scheduler）+ APScheduler（工作日 08:30/16:30 + 启动补跑 + 单飞）+ `/hotspots/run` `/hotspots/status`；web：SSE 通用设施（lib/sse.ts，P4 复用）+ ingest/stream/list 端点 + 首页改造为热点 dashboard（卡片流 + 实时插入 + 手动触发 + 降级标注）。
- 真实跑通：启动补跑自动出 3 条热点（人工智能/人形机器人/水利），相关产品来自板块名匹配基金。
- **技术事实（重要）**：akshare 当前版本 `stock_board_concept_cons_ths` **不存在**（同花顺成分接口不可用）；同花顺板块名单列名是 `['name','code']`（英文）。
- 遗留：股票成分映射待东财恢复（并入 P5 后集中测试）；LLM/Tavily 路径待配置 key 后复验（keyword/国内源回退已验证）。
- 下一步：**P4 统一 Agent**（SSE 设施已就绪可复用）。

## R15 增强：Tavily + 新浪板块成分备源（2026-09-12 用户批准）

- **用户 Tavily key 在 web/.env `TAVILY_API_KEY`（带引号）**：新增 `app/config.py` env 加载器（真实 env > data-service/.env > ../web/.env；TAVILY_API_KEY↔SEARCH_API_KEY 别名；剥引号）。服务内 Tavily 实测生效（newsSource=tavily）。
- **成分映射备源**：新浪 `stock_sector_spot/detail`（84 行业+175 概念，非东财域名）→ 链：东财 cons_em → 新浪（名称精确→包含匹配）。同花顺成分接口当前 akshare 不存在。
- **兜底机制**：关键词命中 <2 → 当日涨幅居前板块补位（新浪 spot 涨跌幅，仍走成分映射）——保证用户不空白。
- **结论：web search 不适合成分映射**（非结构化+代码幻觉风险），结构化板块接口才是正解——已向主人说明。
- 实测：3 条热点各带 6 只成分股（超导概念等）；P3 复跑 27/27 无回归。

## P4 脚手架（🟡 进行中，13/13 部分验收通过）

- 已交付：lib/llm.ts（流式 + tool_calls 解析）、lib/tools.ts（L1 六工具：search_products/get_quote/get_kline/get_hotspots/get_fund_holdings/get_phase_analysis，结果压缩）、lib/chat.ts + sessions API（GET/POST/PUT/DELETE）、/api/chat（SSE + function calling 循环 ≤4 轮 + 意图升档占位）、/chat 页面 + ChatUI + Nav 入口。
- 迁移：ChatMessage 增 toolCallId/name（function calling 配对必需）。
- **阻塞：web/.env 缺 LLM_BASE_URL/LLM_API_KEY/LLM_MODEL** —— 用户提供后重启 dev server 即可端到端验证 function calling（这是 P4 收尾的唯一剩余项）。
- Tavily key 无引号版本已验证（加载器兼容两种写法，无需改代码）。

## P4 完成 ✅（2026-09-12 晚，验收 19/19）

- LLM 配置已由用户提供（deepseek-flash）。排障三连：
  1. **LLM_BASE_URL 带了 /anthropic 前缀**（用户按 DeepSeek 的 Anthropic 兼容文档抄的）→ 404；已修正为官方 OpenAI 协议地址，且 llm.ts 支持 BASE_URL 缺省按模型名推断（deepseek/glm）
  2. **用户重写 .env 时弄丢了 DATABASE_URL** → Prisma 全挂；已补回 file:./dev.db
  3. **Next.js patched fetch 缓冲 SSE 流**：出站 stream:true 请求必须加 `cache: "no-store"`，否则 fetch 永挂——这是 P4 最深的坑，已写入 lib/llm.ts 注释
- 19/19：会话 CRUD、function calling E2E（自动调 get_quote 经腾讯备源回答真实价格）、**多轮上下文指代消解**、LLM 未配置降级指引、/chat SSR。
- 项目根已清理（仅 PLAN/Progress/README/.gitignore）。下一步：**P5 Harness 整合**。

## P4 体验优化（2026-09-12 晚）

- 聊天界面引入 react-markdown + remark-gfm（表格/加粗/列表渲染）；用户气泡限宽、助手消息文档式面板；工具状态单 chip 原地更新（调用→完成）。tsc 0 错 + SSR 渲染断言通过。
- **agent-browser 在沙箱内 daemon 启动失败（socket 限制）**——浏览器自动化在本沙箱不可用，视觉验证改用 react-markdown SSR 断言。

## P5 Harness 整合完成 ✅（2026-09-12 深夜，验收 19/19）

- **M6**：openbb_provider（yfinance 后端，us 类型主源）；沙箱内 Yahoo 限流 → 降级路径实测，正流程待用户网络。
- **M5 B 计划引擎**（custom-multichar）：adapter（进程内 provider 链 + HTTP 回读 web /api/kline——**响应新增 phases 字段，研报与详情页归因同源**）→ 5 角色 LLM 链 → 辩论 → 评级；看门狗超时（akshare 无 timeout 会挂死）；熔断（LLM≤12 次/8 分钟）；code+date 每日限 1 次；ingest 回调落库。
- **TradingAgents spike**：.venv-spike 安装+import 通过（版本冻结）；B 计划为主力，native 后端留作后续。
- **web**：/api/research/start|ingest|GET；L2 工具 deep_research/get_research_report（AGENT_TOOLS=L1+L2）；聊天意图升档真实触发；详情页 ResearchPanel 研报视图（评级徽章/分析师卡/辩论/风控/降级标注）；热点卡"深度解读"激活；ChatUI 研究完成推送。
- **端到端**：600519 研报 rating=谨慎、llmCalls=5、行情 tencent 备源、降级标注（新闻源受东财限流）；技术分析师引用 P2 同源阶段数据；LLM 严守"不编造、缺口标注"话语体系。
- **东财遗留集中测试**：转债 K 线仍不可用（EM 侧，维持搁置）；快照补齐主动跳过（限流期间不刷 1600+ 页），EM 恢复后每日同步自动补。
- **排障沉淀**：线程异常静默（tasks.py 缺 import time → 线程首行 NameError 死亡，任务永远 running）；_key f-string {type} 笔误；进程内最小复现（faulthandler.dump_traceback）是定位线程死因的利器。

## P3–P5 全面评估（2026-09-13 凌晨，用户反馈 P5 研报有问题）

- **已修 3 项 P0**：LLM points 字符串→数组归一化（防前端崩溃）；meta.asOf 数据截至时点；kline.ts 失败负缓存（基金 025449 限流下 10+ 次 502 的锤击教训——整段回源失败 30 分钟窗口抑制）。
- **待办（已给方案，等主人批准）**：基本面维度数据（akshare 财务摘要/腾讯 F10 + peTtm/pb 先喂）；个股新闻备源（巨潮 cninfo）；引擎 prompt 约束（缺口角色禁止方向性结论/辩论只引用真实数据角色）；情绪面缺数据=无法判断（非中性）；P3 LLM 结构化复验；P4 组合链测试；P5 超时熔断实测。

## A/B 方案执行完成（2026-09-13 凌晨，主人批准）

- **A 全部生效**：基本面维度（ths 财务摘要主源/em 备源，最新 4 期）→ 基本面角色引用真实数据（2025 年报营收 1720 亿/-1.20% 等）；巨潮公告 cninfo 作为 A 股新闻降级备源（600519 degraded=false）；话语约束生效（情绪面缺数据=无法判断、辩论仅采信 dataBased 角色、meta.asOf 时点）。
- **B 全部完成**：B1 P3 LLM 结构化复验（engine=llm ✓ Tavily+LLM 5 条热点）；B2 P4 组合链 7/7（get_hotspots+get_quote 多工具）；B3 熔断单测 PASS（env 覆盖+allow 边界+超时）。
- **新发现 bug**：同花顺财务摘要按报告期**升序**返回 → head(4) 取到 1998 年老数据 → 修复为降序取最新 4 期（LLM 自己指出了数据时段问题——话语约束的反向证明）。
- dataBased 字段在重建 analysts 时曾丢失 → 已保留（前端可用于显示数据支撑状态）。
- 项目状态：P0–P5 全部完成（P2 遗留 2 项转债 K 线 + 快照补齐待 EM 恢复）。下一步 P6。
- 沙箱临时文件（safe-delete 的 trash 机制故障）未能自动清理，下次会话可删：web/{npm-install.log,p2-check.json,tsc-out.txt,tsc-exit.txt,test-out.txt,p2-exit.txt}、根目录 {probe.txt,probe2.txt,crypto-probe.txt,debug-result.txt,cleanup-log.txt,port-check.txt}、data-service/{py-probe.txt,err-detail.txt,px-matrix*.txt,probe-px*.py,btc-retry.txt,debug_proxy_server.py}。
