# dependencies
node_modules/
.venv/
.venv-spike/
__pycache__/
*.pyc
*.pyo

# build caches
.next/
out/
*.tsbuildinfo

# env & secrets
.env
.env.local

# database
*.db
*.db-journal
# SQLite WAL 模式边车文件（启用 WAL 后产生；含未检查点数据，不应入库）
*.db-wal
*.db-shm

# misc
.DS_Store
*.log
.next-stale
backups/
docker-compose.override.yml

# CR4（P3）：verify-all.mjs 的产物（跑测试时生成）
verify-suites.txt

# npx skills 的"通用位"产物：只有 Codex 等其他 agent 读它，Claude Code 只读 .claude/skills/
# 技能实体已落在 .claude/skills/<name>/，故此处忽略，避免同一份内容在仓库里存两份
.agents/
